# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# lockfile is intentionally untracked: this project is developed on
# Windows but deploys on Linux, and a single-OS lockfile omits the other
# platform's native bundler binary (npm/cli#4828). CI resolves fresh.
package-lock.json
